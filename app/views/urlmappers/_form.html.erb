
<%= form_for(@urlmapper, :remote=>true) do |f| %>
  
  <div class="text_field">
    <%= f.text_field :url, :class=>"form-control" , placeholder: "Original url"%>
  </div><br/>
  <div class="actions">
    <%= f.submit class:"btn btn-info" , value: "Shorten"%>
  </div>

<% end %>
